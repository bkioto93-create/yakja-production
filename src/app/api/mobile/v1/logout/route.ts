// مسیر فایل: src/app/api/mobile/v1/logout/route.ts
// تسک ۵ فاز M01 (لایه‌ی API موبایل، بخش الف) — نسخه‌ی HTTP-محورِ همان «خروج از حساب»ی که در وب
// با src/app/[lang]/profile/actions.ts::logoutAction انجام می‌شود.
//
// طبق همان اصل بنیادین تسک‌های ۲/۳/۴ همین فاز: هیچ منطق تجاری تازه‌ای اینجا نوشته نمی‌شود — همان
// destroySession() موجود در src/lib/auth/session.ts (که logoutAction وب هم مستقیم صدا می‌زند)
// عیناً فراخوانی می‌شود؛ نیازی به هیچ استخراج/تغییری هم نبود، چون این تابع از قبل بدون هیچ عارضه‌ی
// جانبی مخصوص مرورگر (redirect و...) صادر شده بود — فقط خودِ کوکی نشست را حذف می‌کند.
//
// **چرا logoutAction وب مستقیم فراخوانی نشد:** آن تابع بعد از destroySession()، یک redirect()
// هم اجرا می‌کند (بازگشت به صفحه‌ی خانه‌ی مرورگر) — که در یک Route Handler/اپ موبایل بی‌معناست؛
// دقیقاً همان دلیلی که تسک ۴ باعث شد PATCH پروفایل هم به‌جای switchLanguageAction، تابع مشترک
// زیرینش را صدا بزند.
//
// **نکته‌ی معماری نشست:** نشست این پروژه کاملاً بدون‌حالت (Stateless) و مبتنی بر توکن امضاشده‌ی
// خودکفاست (بدون جدول/لیست نشست‌های فعال در دیتابیس) — یعنی این Route هیچ نشستی را در دیتابیس
// «باطل» نمی‌کند (چون چنین چیزی از اول برای این معماری وجود ندارد). خروج واقعی از حساب در اپ
// موبایل با **حذف توکن از expo-secure-store سمت کلاینت** انجام می‌شود (تسک ۱۰، بخش ب همین فاز)؛
// فراخوانی این Route فقط برای هم‌ترازی رفتاری با logoutAction وب و آماده‌بودن برای هر تغییر آینده‌ی
// معماری نشست (مثلاً افزودن یک لیست سیاه/جدول نشست) نگه داشته شده — دقیقاً همان‌جایی که آن تغییر
// باید اعمال شود، بدون نیاز به لمس کد اپ موبایل.
//
// بدنه‌ی درخواست: ندارد.
// خروجی: همیشه `{ success: true }` — این عملیات idempotent است (حتی بدون نشست معتبر هم خطا
// نمی‌دهد، دقیقاً هم‌رفتار با destroySession() که حذف یک کوکیِ از قبل ناموجود را هم بی‌خطا انجام
// می‌دهد).
import "server-only";
import { NextResponse } from "next/server";
import { destroySession } from "@/lib/auth/session";

export async function POST() {
  await destroySession();
  return NextResponse.json({ success: true });
}