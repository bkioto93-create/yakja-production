// مسیر فایل: src/app/[lang]/loading.tsx
// این فایل، طبق قرارداد Next.js App Router، خودکار روی *همه‌ی* صفحات و تب‌های زیرِ src/app/[lang]
// اعمال می‌شود — یعنی هر صفحه‌ای که Next.js تشخیص دهد برای رندرشدن نیاز به واکشی داده (و در نتیجه
// کمی زمان) دارد، تا وقتی آماده شود، همین لودینگ به‌جایش نشان داده می‌شود. کاربر با کلیک روی هر
// لینک/تب (مثلاً آیتم‌های BottomNav یا هر <Link> دیگری در پروژه)، بلافاصله همین لودینگ را می‌بیند
// و می‌فهمد دارد صفحه عوض می‌شود — نه اینکه چند ثانیه با صفحه‌ی خالی یا بی‌واکنش بماند و دوباره
// روی دکمه بزند.
//
// نکته‌ی مهم فنی: چون این فایل داخل src/app/[lang]/layout.tsx قرار می‌گیرد (یعنی همان
// BottomNav/DesktopHeader سرجایش ثابت می‌ماند)، فقط ناحیه‌ی محتوا (<main>) با این لودینگ عوض
// می‌شود — تجربه‌ای نرم و بدون پرش کل صفحه.
//
// اگر بعداً خواستی یک بخش خاص (مثلاً بازارچه یا املاک) پیام لودینگ اختصاصی‌تری داشته باشد، کافیست
// یک loading.tsx مشابه همین داخل همان پوشه (مثلاً src/app/[lang]/marketplace/loading.tsx) بسازی؛
// Next.js خودش نزدیک‌ترین loading.tsx به هر مسیر را انتخاب می‌کند.
import { PageLoader } from "@/components/ui/PageLoader";

export default function LangLoading() {
  return <PageLoader />;
}
